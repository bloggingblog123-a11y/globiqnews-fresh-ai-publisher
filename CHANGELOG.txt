6.1.0 — 2026-09-26
Independent headline engine; source/site/live-job title comparisons; bounded title regeneration; copied-sentence and meta/excerpt checks; numeric/quotation integrity guards; title-only actions; source-prose isolation; factual/originality gate before SEO repair; neutral title cosmetics optional. Image behavior and Draft-only publishing preserved. See TEST-REPORT-6.1.0.md.

6.0.6 — 2026-09-26
Prioritize opening focus keyword, supported expansion to 600+ words and factual title numbers. Preserve passing text checks; allow partial length progress; one refreshed bounded policy budget for older unedited drafts. No image changes or auto-publishing.

6.0.5 — 2026-09-24
Prevent stale category/global forms from overwriting newer saves. Verify category database persistence; reject empty/truncated requests; display local save feedback. Image/link scopes and Draft-only queue preserved. See TEST-REPORT-6.0.5.md.

6.0.4 — 2026-09-24
Persistent category queue; immediate background handoff; concurrency 1–2; atomic claims and ownership-safe cleanup; terminal/failure/stale worker recovery; independent six-hour source retries; current RSS settings authoritative; no hidden HTML feed discovery; richer source diagnostics. Draft-only preserved. See TEST-REPORT-6.0.4.md.

6.0.3 — 2026-09-23
Atomic shared settings write lock prevents overlapping global/category/subsection saves from silently losing changes. Source tests include enabled manual external links, skip disabled entries, report OFF/empty states, deduplicate legacy URLs and identify research links. See TEST-REPORT-6.0.3.md.

6.0.2 — 2026-09-22
Independent image/manual-link AJAX saves, stale-section protection and unsaved warnings; image slot and WebP/JPEG preferences; public research links OFF by default; optional category-only contextual manual links; independent internal linking and private reports; Draft-only publishing preserved. See TEST-REPORT-6.0.2.md.

6.0.1 — 2026-09-22
Fix external-link insertion; broaden relevant internal linking; run non-image SEO preflight without Node/remote scoring; add Improve SEO & Links and a private checklist; include metadata in factual review; bound unproductive repairs; correct busy/error status reporting. Images and manual edits preserved. Draft-only publishing retained. See TEST-REPORT-6.0.1.md.

6.0.0 — 2026-09-21
Research-first original Drafts; GDELT; fact sheets and private quality reports; optional original WebP images; manual-edit/media protection; bounded batch limits and safe retries. All automatic publication removed. Existing settings and schedules preserved through versioned migration. See TEST-REPORT-6.0.0.md.

Version 5.31.0 - HTTPS scoring for shared hosting

Adds an optional authenticated HTTPS service so WordPress can obtain genuine
background Rank Math scores without Node.js or PHP proc_open on its host.
Uses the same verified Rank Math Free 1.0.278 analyzer and WordPress dependencies;
the supplied runtime is verified against WordPress 7.1. Different hashes fail closed.
Requests/results are authenticated and bound to unique IDs and the final article.
Remote mode requires saved article-sharing permission. API keys, passwords, local
paths and executable code are excluded. One bounded worker performs analysis.
The service does not store articles or publish posts. Logs exclude article data.
Cold starts, timeouts and invalid responses leave Draft, with at most 3 attempts.
Adds service settings and a Render Free deployment template. Existing settings,
API keys and categories persist. See TEST-REPORT-5.31.0.md and scoring-service/README.md.
This staging candidate is not yet verified on the user's live MilesWeb site.

Version 5.30.0 - Genuine background Rank Math analysis

Runs the installed Rank Math Free 1.0.278 analyzer with Node.js after final article
text, slug, metadata, links, image attachments and ALT text have been saved.
The saved numeric score must be verified for those inputs and at least 80 before
automatic publication. Missing, invalid, stale, failed and lower scores stay Draft.

Adds bounded scoring retries (3 attempts), one optional existing-writer SEO repair,
metadata preservation, source/engine fingerprints, a central guarded publish path,
and one article worker across categories. The repair reuses existing images.
Adds hosting compatibility information, SEO status, retry count and failure reason
in settings. Saving settings and opening settings no longer perform slow analysis.
Settings, API keys, category sources, schedules and the GitHub updater are preserved.

Hosting requirements: Node.js 18+ executable, PHP proc_open, writable temporary
storage and Rank Math Free 1.0.278 with the verified analyzer. Other analyzer
versions and Rank Math PRO are not yet supported. No guessed score is substituted.
Use Analyze & Publish 80+ Drafts Now to retry after fixing runtime availability.

Validation: isolated WordPress 7.1 / PHP 8.4.25 / Rank Math 1.0.278; genuine scores
76,79,80,81,84,85; missing/invalid/noindex/disabled/unavailable states; stale article,
metadata, taxonomy and ALT changes; 3-attempt bound; category serialization;
existing finalizer and one-repair path with mocked Gemini transport and real local
attachments; settings preservation. Actual Gutenberg editor and Posts SEO Details
both displayed 80/100 for the article scored 80 by the background worker.
Live MilesWeb execution and third-party editor JavaScript customizations have not
been verified. Confirm hosting support on staging before enabling live updates.


Historical changes

GlobiqNews Fresh AI Publisher V5.28 - Existing 80+ Draft Publishing Fix

INSTALL AND RECHECK
Upload this ZIP through WordPress Plugins > Add New > Upload Plugin, replace
the existing plugin, and activate it. Enable Auto Publish and Save Settings.
Enable the recovered-draft publishing option if those drafts should publish too.
Click "Check & Publish 80+ Drafts Now" to process a batch of existing drafts.
The action can publish eligible articles publicly. Refresh to update the list.

FIXES
- Uses Rank Math's actual saved rank_math_seo_score as the score source.
- Removes the private fingerprint requirement that silently blocked 80+ drafts.
- Includes completed drafts created while Auto Publish was OFF and older drafts
  blocked by strict validation. Strict validation is still not required.
- Handles saving an unchanged score: WordPress may skip updated_post_meta for
  identical values, so the plugin also observes update attempts and post saves.
- Rechecks eligible saved scores after settings changes, on this plugin's admin
  page, through the recheck button, and in the existing WP-Cron worker.
- Batch scanning advances past blocked drafts so later articles are not starved.
- Draft Auto Publish Status shows the saved score and explicit blocking reasons.
- Save Settings is retained. Existing API keys and settings remain unchanged.

SCORE AND STATUS DETAILS
An unsaved score shown in the Rank Math editor is not necessarily stored in
WordPress yet. Save the article, then use the recheck button. This plugin does
not calculate or fabricate the Rank Math score. It accepts a valid saved score
from 80 through 100 without additional private score-tracking requirements.
Generation/repair still clears the previous score when rewriting the article.
After editing content, save/recalculate with Rank Math to keep its score current.
Source: https://developer.wordpress.org/reference/functions/update_metadata/

Only this plugin's completed Draft articles qualify. Active/incomplete processing,
skipped articles, unrelated posts, and other statuses are not automatically
published. Previously auto-published articles manually returned to Draft stay
Draft. Recovered drafts respect their separate publishing switch. Processing
failures still use the existing recovery system. WP-Cron depends on site traffic.

VERIFICATION
82 assertions passed in a WordPress stub harness, including reproduction of a
completed draft with saved score 85 and missing private metadata, unchanged-score
saves, existing drafts, 79.99 vs 80, missing Rank Math, settings, database errors,
locks, batch pagination and generation/recovery paths. PHP and JavaScript syntax
checks passed. This is not a live integration test on the user's WordPress site.

Historical release notes below are superseded by the V5.28 behavior above.

GlobiqNews Fresh AI Publisher V5.27 - Rank Math 80+ Auto Publish

The current publishing rule replaces V5.26's strict validation + 83 requirement.
Enable Auto Publish and save settings. Completed generated drafts publish when
their actual saved Rank Math SEO score reaches 80/100 or more. Exactly 80 counts.
The strict-check publishing gate and strict-validation repair loop are removed.
SEO writing targets still guide generation but are not publishing requirements.
Missing, invalid, outdated and below-80 scores keep posts as Draft.

Rank Math must calculate and save the score first. Open and save a waiting draft
with Rank Math active. This plugin does not run Rank Math's JavaScript analyzer
in WP-Cron and does not invent SEO scores. After manual content edits, a fresh
score can qualify the updated article without repeating strict validation.

Auto Publish OFF and recovered-draft publishing preferences remain effective.
Image/article generation still completes before a draft enters score waiting;
processing failures continue to use recovery. Existing waiting drafts use 80+
automatically. Drafts blocked by the old strict gate can qualify after Rank Math
saves a fresh score, if recovered-draft publishing is enabled. Skipped drafts
and unrelated posts remain excluded. The Save Settings button is retained.

Verification: PHP syntax checks and a stubbed WordPress test harness cover the
80 boundary, old strict failures, generation, fresh/stale scores and OFF settings.
No live WordPress/Rank Math integration test was performed on your website.

Historical release notes follow. Their older thresholds and strict publishing
requirements are superseded by V5.27 above.

GlobiqNews Fresh AI Publisher V5.26.1 - Save Settings Button

Adds a Save Settings button beside Test Gemini Connection in the Gemini Article
Writer section. It submits the existing WordPress settings form, including all
page settings. Save before testing the connection. Save feedback is shown at
the top of the settings page. The V5.26 Rank Math 83+ publishing gate is retained.

GlobiqNews Fresh AI Publisher V5.26 - Rank Math 83+ Auto Publish

INSTALL / ENABLE
1. Upload the V5.26 ZIP through WordPress Plugins > Add New > Upload Plugin.
2. Replace the existing GlobiqNews Fresh AI Publisher installation and activate it.
3. In GlobiqNews Fresh AI settings, enable "Auto Publish when Rank Math SEO score
   is 83 or more AND all strict checks pass", then save settings.
4. Leave "Auto-publish recovered drafts" enabled if recovery should publish too.

PUBLISHING RULE
- All existing strict validation checks must pass AND Rank Math's real saved
  rank_math_seo_score must be at least 83/100. Exactly 83 qualifies.
- A missing, invalid, stale or lower score leaves the article as Draft.
- Existing Auto Publish OFF settings and Draft/Pending choices are preserved.
- Applies to articles finalized/recovered by V5.26. Old completed drafts are not
  automatically enrolled or published just by installing the update.

SCORE CALCULATION
Rank Math calculates its SEO score using its JavaScript analyzer. This update
does not run that analyzer in WP-Cron or invent a score from the strict checklist.
After generation, use the new "Waiting for Rank Math 83+" section to open and
save the draft with Rank Math active. Once Rank Math saves a qualifying score,
the plugin rechecks all strict checks and publishes automatically.
WP-Cron also retries eligible waiting drafts approximately every 15 minutes,
subject to normal WordPress traffic/cron scheduling. It does not calculate scores.
Reference: https://rankmath.com/kb/seo-score-not-available/

RELIABILITY
- Waiting for a score does not consume AI recovery attempts or regenerate images.
- Repairs clear the previous score. Content/SEO metadata changes invalidate the
  validation snapshot and require recovery plus a fresh Rank Math score.
- Unrelated, skipped, scheduled, private, trashed or published posts are excluded.
- Recovery facts are retained while waiting and removed after successful publish.
- Settings and API keys retain their existing option names; no credentials added.

VERIFICATION
All 9 PHP files passed syntax checks with PHP 8.4.25. 62 publishing-gate and
runner assertions passed using a WordPress stub harness, including the exact 83
boundary, strict-check failure, missing Rank Math, OFF settings, recovered drafts,
stale scores, repeat events, database write failure and concurrent changes.
This is not a live WordPress/Rank Math integration test on your website.

Historical release notes below describe previous versions; V5.26 rules above
supersede their earlier publish-only-after-strict-validation descriptions.

GlobiqNews Fresh AI Publisher V5.25 — Bulk Recovery Queue

This build was made after another full section-by-section audit of V5.20. V5.21 moves the WordPress Draft checkpoint ahead of optional Gemini repair calls, adds atomic category/source concurrency claims, makes active processing recoverable after hard interruption, fixes the real image retry-before-fallback behavior, strengthens source redirect handling, and hardens the 2-image/link pipeline. See CHECK-AUDIT-V5.21.txt for the current audit.

Corrections beyond V5.12:
- Fixed a Source URL false-positive: category pages containing many JSON-LD NewsArticle descriptions are no longer mistaken for one direct article. Only real articleBody JSON-LD can short-circuit direct-article detection.
- Category discovery ignores nav/header/footer/sidebar links and strongly prefers paths matching the configured category URL.
- External links now insert only statuses OK or RESTRICTED; broken/unavailable links are skipped.
- Source URL comparison handles trailing-slash/resource variants more safely.
- 1000–1200 word validation now measures the actual written article body, not generated TOC/Related Reading/Useful Resources navigation.
- First-10%, conclusion, heading, FAQ and paragraph readability checks now inspect the article body rather than generated navigation.
- Rank Math TOC serialization now follows Rank Math's current converter structure more closely: minimal heading attributes and no extra TOC div ID.
- Gutenberg list blocks now use nested core/list-item blocks to reduce "unexpected or invalid content" errors.
- Strict validation cannot be bypassed by switching off Rank Math or the required 2-image system.
- Internal linking cannot be bypassed when a same-category published post is available.
- WordPress title is explicitly required in the writing prompt to contain the focus keyword.
- If no source-supported title number exists, the current publication year is the only allowed fallback number.
- Category author picker now shows only users who can edit posts and includes valid login/display data.

All V5.12 features remain, including V5.9 automatic recovery, V5.11 RSS/Source/External engine, custom instructions, category save/author/limit/timing, 2 low-storage original images and Auto Publish strict gate.

Important: the plugin performs its own strict Rank Math-style validation and writes Rank Math metadata. Rank Math's Content AI is separate, and Rank Math's internal analyzer/power-word dictionary can change between plugin releases; no third-party plugin can honestly guarantee every Rank Math UI test will always show green without running that exact Rank Math analyzer at runtime.

GlobiqNews Fresh AI Publisher V5.12 — Auto Publish + Full Requirements

NEW IN V5.12
- Dedicated Auto Publish section. Articles are always created as Draft first. Auto Publish happens only after the full strict gate passes.
- Upgrade-safe migration from V5.11 Final Status: an old Publish setting becomes Auto Publish ON; Draft/Pending behavior is preserved.
- V5.9 automatic recovery remains: retries around 15 minutes, 1 hour and 6 hours, with successful article/image state reused.
- Recovered drafts can auto-publish after they pass the same gate.
- Rank Math-style keyword density calculation corrected to exact phrase occurrences / total words.
- Expanded strict Rank Math/on-page validation: focus keyword begins SEO title; keyword in WordPress title/meta/slug/first 10%/H2-H3/body/conclusion/image ALT; unique title; number/year; power word; one truthful sentiment word; 1000-1200 words; 1.0-1.5% density; TOC; exactly 3 FAQ questions; 5-8 tags; list; short paragraphs; 2 images; internal link when available; supplied external link DoFollow check; Gutenberg structure; schema recommendation; source URL hidden.
- All V5.11 verified RSS/Atom, Source URL and Trusted External Link benefits retained.
- All V5.7 custom instructions and recovery/checkpoint behavior retained.

GlobiqNews Fresh AI Publisher V5.11 — Source Verification

Source-system corrections:
- RSS / Atom: WordPress parser first, raw XML fallback, Atom alternate links preferred over rel=self.
- Source URL: direct article, category/listing page and feed URL are auto-detected.
- Category/listing pages are no longer treated as direct articles merely because they contain NewsArticle cards.
- Source URL discovery continues through same-site links, semantic article cards, JSON-LD, declared feeds and public sitemaps.
- Trusted External DoFollow Links remain category-specific and are saved with each category.
- Test RSS + Sources + External Links checks all three sections independently.
- Known 404/410 external links are not inserted. 401/403/429 is reported as restricted because it may be bot-only blocking.
- Source article URL is never automatically inserted as a visible external link.
- All V5.7 Instructions + Recovery behavior remains retained.

GlobiqNews Fresh AI Publisher V5.10 — RSS + Source URL Engine Fix

SOURCE ENGINE CHANGES
- RSS/Atom section: WordPress SimplePie first, direct raw XML fallback second.
- RSS/Atom/RDF feeds supported.
- If an HTML page is accidentally put in RSS, the plugin can use its declared feed link.
- Source URLs now auto-detect: category/listing page, direct article URL, or feed URL.
- Direct article URLs entered in a category Source box are processed directly.
- Category discovery now reads normal links, semantic <article> links, JSON-LD ItemList/Article URLs, and same-host URLs exposed in serialized page data.
- Public sitemap fallback now supports sitemap indexes recursively.
- More article-body HTML selectors added.
- Functional query parameters such as source/ref/campaign are preserved; only well-known tracking parameters are stripped.
- New per-category "Test RSS + Sources" button checks saved source configuration without creating posts.
- Safe-source behavior is unchanged: 401/403/429/CAPTCHA/anti-bot challenges are skipped, not bypassed.

V5.7 INSTRUCTIONS + RECOVERY PRESERVED
- Global Article/SEO/Image Instructions preserved.
- Per-category custom instructions preserved.
- Draft checkpoint, Gemini retry, image retry/checkpoint, SEO repair and failed-draft recovery preserved.
- V5.9 automatic recovery remains enabled/configurable.
- All existing V5 settings use the same gnf5_fresh_settings option and are preserved on upgrade.

GlobiqNews Fresh AI Publisher V5.9 — Automatic Recovery

New in V5.9:
- Failed image, Rank Math/SEO validation, or post-processing drafts recover automatically.
- Automatic recovery runs every ~15 minutes through WordPress Cron.
- Backoff: first retry ~15 minutes, second ~1 hour, third ~6 hours.
- Default maximum automatic attempts: 3 (configurable 1–5 under Advanced source safety & recovery).
- Successful images and saved article text are reused; only unfinished work is retried.
- Drafts are never deleted by recovery.
- The manual Failed Draft Recovery panel stays hidden unless automatic recovery has exhausted all configured attempts.
- Manual Retry Now remains available only for drafts that still need attention.
- Existing V5.8 API keys, category settings, authors, limits, timing, custom instructions, Rank Math rules, 2-image low-storage setup, source safety and logs are preserved.

GlobiqNews Fresh AI Publisher V5.8 — Auto-Hide Recovery

V5.8 UI refinement:
- Failed Draft Recovery is completely hidden when there are zero failed drafts.
- Recovery Queue diagnostic appears only when a failed draft exists.
- If the final recovery item is skipped, the recovery panel hides immediately.
- All V5.7 settings, custom instructions, API keys, category configuration, retry logic, Rank Math rules, 2-image low-storage system, and recovery engine are preserved.

GlobiqNews Fresh AI Publisher V5.7 — Instructions + Recovery

UPGRADE / SETTINGS
- V5.7 uses the SAME WordPress settings option as V5.6: gnf5_fresh_settings.
- Existing Gemini/OpenAI keys, image settings, categories, authors, RSS feeds, source URLs, post limits, timing, blocked retry delay and Rank Math settings are preserved.
- New instruction fields default to blank, so upgrading does not change working behavior until you add instructions.

LOCKED REQUIREMENTS
- Category-wise isolation: RSS/source URLs never mix categories.
- Per-category author, target post limit, automatic timing and separate Save Category button.
- RSS optional; URL-only mode supported.
- 1000–1200 words, target about 1100.
- Strict Rank Math validation and repair.
- Unique SEO title; focus keyword placement/density; number/year; power word; one truthful sentiment word; TOC; FAQ; tags; ALT.
- Exactly 2 generated images total: image 1 featured+inline, image 2 inline.
- Low-storage 1200x675 WebP when PHP GD/WebP is available.
- Source/RSS images are not downloaded, copied, traced, transformed, or used as image references.
- Blocked source retry delay defaults to 6 hours; anti-bot systems are not bypassed.

CUSTOM INSTRUCTIONS
- Global Article Instructions
- Global SEO Instructions
- Global Image Instructions
- Per-category Custom Instructions
- New saved instructions apply automatically to future generated articles.
- Locked factual/word-count/image/category/critical-SEO rules override conflicting custom instructions.

FAULT TOLERANCE / RECOVERY
- Gemini retries up to 3 times; optional backup model after primary retries fail.
- Article is saved to WordPress Draft immediately after text generation succeeds.
- Image generation retries up to 3 times.
- Each successful image is checkpointed. If one image fails, Retry generates only the missing image.
- SEO/readability validation receives up to 2 automatic repair passes.
- Processing state is saved on the post.
- Failed Draft Recovery panel provides Retry Now, Skip Recovery and Edit Draft.
- Retry reuses stored article data and successful images instead of starting over.

FIRST TEST
1. Keep Final Status = Draft.
2. Configure one category, Target Posts = 1.
3. Save that category.
4. Test Gemini, source extraction and image generator.
5. Run one category.
6. Check Failed Draft Recovery and Live Log if the post remains Draft.

IMPORTANT
- No plugin can guarantee a literal 100/100 Rank Math score across every Rank Math version/module/Content AI configuration.
- This plugin targets the on-page checks listed above and keeps failures as Draft.
